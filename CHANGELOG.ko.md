# 변경 이력 (Changelog)

이 프로젝트의 모든 주목할 만한 변경 사항은 이 파일에 문서화됩니다.

이 형식은 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)를 기반으로 합니다.

## [v2.8.4] - 2026-09-07

### Added (추가 기능)

- **팀 채팅 (P2P MVP)**: SMS/카카오톡형 인박스·방 창(DM·그룹). 메시지 본문은 기기에만 두고, Supabase는 시그널링(`device_keys`, `peer_sessions`, `chat_rooms` 메타)만 사용.
- **E2E 암호화**: 기기별 X25519, DM은 ECDH+HKDF(userId는 salt만), AES-GCM seal/open, 그룹 방키 wrap.
- **LAN → 터널 연결**: TCP 리스너가 LAN을 먼저 광고하고, 실패 시 터널/outbox로 재시도.
- **의사소통 액션 오버레이**: 공통 tiny-skia 엔진 + Windows click-through layered HWND(macOS/Linux는 degrade). 채팅 칩으로 짧은 중첩 애니 재생, 입력 포커스 유지.

### Fixed (버그 수정)

- **커무니케이션 오버레이 크래시**: Windows presenter가 Tauri 커맨드 스레드에서 HWND를 만지지 않도록 분리하고, 래스터/DIB 버퍼 재사용·해상도 상한으로 액션 재생 시 OOM 종료를 막음.
- **멤버 목록에서 소유자 누락**: 백필 마이그레이션 + `listMembers`가 `workspace_members`에 owner 행이 없을 때 `workspaces.owner_id`를 합성 표시.

## [v2.8.3] - 2026-09-04

### Added (추가 기능)

- **OS 네이티브 화면 캡처 & GitHub 버그 리포트**: Win32 GDI 기반 픽셀 완벽 앱 창 즉시 캡처(`capture_app_screenshot`), Windows 캡처 도구 연동(`trigger_os_snip`), 클립보드 붙여넣기(`Ctrl+V`), 시스템 환경 정보가 자동 채워진 GitHub Issue 원클릭 생성 및 인앱 피드백 기능 추가 (`Ctrl+Shift+B`).

### Fixed (버그 수정)

- **로컬 역방향 프록시 타임아웃 및 스트리밍 안정화**: 로컬 업스트림 라우트(`local_origin`)에 대해 충분한 600초 타임아웃을 적용하여 무거운 Next.js RSC 컴파일 및 비동기 데이터 페칭 중 조기 502 Bad Gateway 타임아웃 발생 방지.
- **POST 요청 포워딩 및 HTTP Request Smuggling 오류 해결**: 요청 바디 버퍼링 포워딩 및 중복 `Transfer-Encoding` / `Content-Length` 헤더 전달 제외를 통해 Node.js / Next.js `llhttp` 파서의 소켓 강제 종료(`HPE_INVALID_CONTENT_LENGTH`) 해결.
- **리다이렉트 Location 헤더 자동 변환**: Next.js 로컬 리다이렉트(`302/303`) 응답의 `http://localhost:3000/...` 또는 `http://127.0.0.1:3000/...` 헤더를 가상 호스트 도메인(`https://www.modetour.dev/...`)으로 자동 치환하여 브라우저의 프록시 이탈 방지.
- **Localhost IPv6 DNS 지연 방지**: Windows IPv6 `::1` 조회 지연을 방지하기 위해 `target_host: "localhost"`를 `127.0.0.1`로 정규화하고, 명시적 포트(예: `localhost:3000`) 요청 시 첫 번째 라우트로 오라우팅되지 않도록 보존.
- **Windows 인앱 업데이트 UAC 권한 상승 및 파일 잠금 방지**: `hooks.nsh`에 `NSIS_HOOK_PREINSTALL`을 추가하여 업데이트 설치 전 백그라운드 프로세스(`horizon-gateway-serve.exe`, `hgc.exe`) 및 `WinDivert` 드라이버를 강제 종료하고, NSIS `installMode: "perMachine"`을 지정하여 UAC 관리자 권한 상승을 보장하도록 개선.

## [v2.8.2] - 2026-09-01

### Added (추가 기능)

- **API 로그 보존 정책 (Retention Policy)**: 로그 보존 주기 및 한도 설정 기능과 실시간 UI 동기화 추가.
- **비동기 SQLite FTS 배치 인덱싱**: 프록시 처리 지연을 방지하기 위한 백그라운드 MPSC 큐 및 단일 트랜잭션 기반 FTS 인덱싱 워커 도입.

### Changed (개선 사항)

- **프록시 로깅 및 메모리 최적화**: 선택적 JSON/텍스트 바디 로깅 및 2MB 페이로드 상한 적용으로 I/O 병목 방지.
- **인증서 LRU 캐시**: `HostCertCache`에 최대 500개 호스트 LRU 캐시 정책을 적용하여 장기 실행 시 메모리 절감.
- **프론트엔드 렌더링 성능 최적화**: `DomainApiLogsPanel` 및 `JsonTreeView` 대용량 렌더링 가상화/최적화.
- **코드 린트 및 정적 분석 강화**: Biome `noExplicitAny` 규칙 적용 및 Rust 크레이트 Clippy 경고 정리.
- **웹사이트 및 문서 최신화**: 애니메이션 로고 및 중앙 히어로 섹션 개편, GTM 포지셔닝 및 UI 스크린샷 갱신.

### Fixed (버그 수정)

- **WinDivert NAT 세션 정리 및 localhost 연결 안정화**: TCP `FIN`/`RST` 패킷 감지 즉시 해제 및 120초 TTL sweep 적용으로 localhost 프록시 멈춤/연결 끊김 문제 해결.
- **테마 동기화 프록시 포트 동적 확인**: 사용자 지정 프록시 포트에서도 `/.horizon-gateway/api/theme` 요청이 유실되지 않도록 동적 포트 폴백 적용.
- **DaisyUI 테마 CSS 우선순위 충돌 및 태그 정리**: `:root` 및 `[data-theme]`에 `!important` 주입 및 레거시 `#dynamic-theme` 태그 제거로 테마 깨짐 방지.
- **파이프라인 템플릿 변수 보간 수정**: 연속된 템플릿 변수가 치환되지 않던 버그 수정.
- **Mock 규칙 헤더 및 쿠키 정규화**: 비-ASCII 헤더의 URL 인코딩 폴백 및 `Set-Cookie` 속성 대소문자 무관 정규화 지원.

## [v2.8.1] - 2026-08-18

### Fixed (버그 수정)

- **레거시 데이터 마이그레이션**: Watchtower → Horizon Gateway 마이그레이션이 새 `domains.json`이 이미 존재해도 hostname 기반으로 병합하도록 개선했습니다. 기존 스토어에 없는 hostname은 새 ID를 부여해 추가하고, 중복 hostname은 그대로 유지합니다. 도메인 외 다른 설정 파일(그룹, 라우트, mock 규칙 등)은 대상 디렉터리에 없을 때만 복사합니다.
- **마이그레이션 후 정리**: 마이그레이션 완료 후 기존 `com.lurain.watchtower` 디렉터리를 `com.lurain.watchtower.migrated`로 이름 변경하여 중복 마이그레이션 및 구 데이터 참조를 방지합니다.
- **Pull 시 도메인 ID 충돌**: 워크스페이스 Pull 시 원격 도메인 ID가 로컬 ID와 겹쳐 도메인 목록이 손상되던 문제를 수정했습니다. 새로 가져오는 도메인에는 빈 로컬 ID를 할당합니다.
- **Pull 후 동기화 패널 미갱신**: push/pull/서버 편집 후 카운터만 증가시키고 스냅샷을 리로드하지 않아 UI가 수동 새로고침 전까지 갱신되지 않던 문제를 수정했습니다.
- **Pull 시 도메인 전체 삭제**: `importAllSettings` IPC 디스패치가 Tauri 인자 래퍼 `{ payload, mode }` 전체를 `SettingsExport`로 역직렬화해, `domains`·`groups` 등 모든 필드가 빈 배열로 초기화되어 Pull 때마다 모든 데이터가 삭제되던 문제를 수정했습니다. 이제 inner `payload` 객체와 `mode` 문자열을 올바르게 분리해 처리합니다.

## [v2.8.0] - 2026-08-18

### Added (추가 기능)

- **도메인 일괄 IPC (Bulk domain IPC)**: `remove_domains`, `set_domain_groups`, `set_domain_api_logging`, `remove_domain_api_logging`, `update_local_route`, `set_https_decrypt_host`에 일괄 처리(`ids`/`domainIds`/`hosts`)를 지원하여 한 번의 IPC 호출로 다수 도메인을 처리합니다.
- **업데이트 전환 (`prepare_for_update`)**: GUI가 serve 사이드카 프로세스를 정상 종료 후 대기하여 Windows 파일 락 충돌을 방지합니다.
- **Serve 수명주기 이벤트**: `serve-ready` 및 `backend-unavailable` 이벤트를 도입하여 백엔드 재연결 시 부트스트랩 및 구독을 자동으로 재시도합니다.
- **가이드 기능 카탈로그 & 링크**: `[[` 별칭을 확장하여 마크다운에서 `hg://domain/:id/:panel` 및 글로벌 도구를 직접 연결합니다.
- **인스펙터 전면 테마 토큰화 & 프록시 테마 동기화**: 인스펙터의 모든 컴포넌트(가이드, 모달, 팝오버, JSON/헤더 뷰어, 플로팅 핀, 에디터)에 CSS 테마 토큰 변수를 적용하고, `/.horizon-gateway/api/theme` 및 `theme.json` 영속화를 통해 실시간 테마 동기화를 지원합니다.
- **PAC TLS 바이패스 목록**: 생성된 PAC 스크립트가 설정된 `tlsBypassHosts`를 준수하도록 확장했습니다.
- **기본 TLS 바이패스 시드**: Teams, Slack, Zoom, Discord 등의 업무 도구 호스트를 기본 포함합니다.

### Changed (변경 사항)

- **인스펙터 SVG 아이콘 통일**: 텍스트 및 이모지 아이콘을 Lucide SVG 아이콘으로 교체하고 접힘 모드 로고를 미니멀 아이콘으로 정비했습니다.
- **커스텀 테마 (DaisyUI 5)**: `:root` 및 `[data-theme="<id>"]`에 CSS 변수를 동적 주입하여 컴파일된 테마와의 우선순위 충돌을 방지했습니다.
- **기본 타이포그래피**: 라이트/다크 기본 프리셋의 기본 폰트 크기 13px, 줄 간격 1.4로 조정했습니다.
- **설정 UI & 프록시 설정**: 카드 외부 섹션 타이틀 레이아웃(`watchtower-ui`)과 여백 리듬을 정비했습니다.

### Fixed (버그 수정)

- **GUI 시작 경쟁 상태**: 초기 앱 데이터 로드 실패 시 백오프 재시도를 수행하여 serve 준비 완료 시 정상 복구됩니다.
- **어노테이션 무한 폴링 방지**: SSE 스트림 안정화 및 브라우저 캐시 방지 헤더/쿼리를 강화했습니다.

## [v2.7.10] - 2026-08-14

### Added (추가 기능)

- **허브 오버뷰 컨트롤**: HTTPS 복호화와 도움말, 인젝션 토글, 로컬 목적지, inspect 목록을 허브 오버뷰에서 다룹니다.
- **가이드 에디터**: CodeMirror에 탭 들여쓰기, 자동 쌍 입력, `[[` 기능 링크(`hg://`), 인젝션에서 재캡처, 상세 오류 토스트를 넣었습니다.

### Changed (변경 사항)

- **호스트 행 단위 토글**: 라우팅·모킹·인스펙터를 호스트 행에서 켭니다. 전역 마스터 스위치는 제거했습니다.
- **가이드 호스트 필터**: 패턴만으로 매칭하고, 가이드 팝오버 레이아웃을 정리했습니다.
- **DNS A/CNAME 존**: 설정과 프록시 DNS 엔진에서 제거했습니다.
- **유료 결제**: 프로덕션 빌드에서는 결제 체크아웃을 막아 두었습니다.
- **`tauri dev` leftover serve**: 개발 시작 시 남은 `horizon-gateway-serve`를 정리합니다. 프로덕션은 계속 트레이로 숨깁니다.

### Fixed (버그 수정)

- **8888 포트 프록시 시작**: 포트가 이미 이 앱이 잡고 있을 때 프록시 시작이 더 안정적입니다.

## [v2.7.9] - 2026-08-13

### Added (추가 기능)

- **닫기 / 최소화 동작 선택**: 메인 창 닫기는 트레이로 숨기거나 앱을 종료할 수 있고, “이 선택을 다음에 그대로 사용”을 지원합니다. 최소화는 작업 표시줄에 남기거나 트레이로 숨길 수 있습니다. 설정에서도 동일 옵션(매번 묻기 포함)을 제공합니다.
- **어노테이션 SSE**: 인젝션 페이지가 2초 폴링 대신 `/.horizon-gateway/api/annotations/stream`을 구독하며, EventSource 오류 시 폴링으로 폴백합니다.

### Changed (변경 사항)

- **Policies를 허브 오버레이로 이동**: 독립 라우트 대신 허브 서페이스에서 UI/UX 정책을 엽니다.
- **창 버튼 문구 통일**: 트레이로 숨기기와 작업 표시줄로 최소화를 같은 말로 구분합니다.

### Fixed (버그 수정)

- **GUI 셸에 인스펙터 툴바 노출**: 프로덕션 Vite가 `inspector.js`를 앱 창에 넣지 않습니다. 인젝션은 별도 빌드이며, Tauri/localhost 셸 호스트에는 주입하지 않습니다.

## [v2.7.8] - 2026-08-13

### Added (추가 기능)

- **`hgc` 콘솔 CLI**: UAC 없는 asInvoker 클라이언트입니다. `hgc init`, `hgc list`, `hgc help <command>`, `hgc <command>`(`run` 생략 가능). `horizon-gateway cli …`는 호환 래퍼로 `hgc`를 실행합니다.
- **Serve / GUI 프로세스 분리**: 헤드리스 `horizon-gateway-serve`가 프록시·저장소·IPC를 담당하고, Tauri GUI는 serve를 띄운 뒤 localhost IPC로 invoke를 전달하는 얇은 셸입니다.

### Changed (변경 사항)

- **Windows 관리자 매니페스트를 serve에만 적용**: `requireAdministrator`는 `horizon-gateway-serve`에만 붙고, `hgc`와 라이브러리 테스트에는 붙지 않습니다.
- **에이전트 스킬 문서**의 기본 명령 형식을 `hgc`로 바꿨습니다.

### Fixed (버그 수정)

- **실행 중인 백엔드에서 `--query` 미적용**: serve IPC 경로에서도 headless `run`과 같이 `--query`가 동작합니다.
- **Windows `tauri dev` 재빌드 루프**: 디버그 빌드가 watched `resources/`에 `serve.exe`를 복사하지 않도록 바꿔, 재빌드 루프와 Common Controls v6 매니페스트 누락을 막았습니다.
- **macOS / Linux 릴리스 번들**: sidecar를 Windows `.exe` 리소스가 아니라 `externalBin`으로 넣어, Unix `tauri build`가 `horizon-gateway-serve.exe`를 찾지 않게 했습니다.

## [v2.7.7] - 2026-08-11

### Added (추가 기능)

- **WinDivert 기반 Transparent Proxy 커맨드 (`start_transparent_proxy`, `stop_transparent_proxy`, `get_transparent_proxy_status`)**: Windows 환경에서 Node.js(undici fetch), Java, Python 등 OS WinINet 프록시를 따르지 않는 프로세스의 아웃바운드 트래픽(HTTP 80 / HTTPS 443)을 가로채는 Transparent Proxy 서비스 및 CLI 커맨드를 추가했습니다.
- **Outbound IP/TCP Destination NAT 변환**: `etherparse` 기반 패킷 헤더 실시간 변환(`rewrite_ipv4_tcp_dst`)과 세션 트래킹 매핑(`NAT_TABLE`)을 연동하여 아웃바운드 패킷을 Horizon Local Proxy 포트로 자동 리다이렉트합니다.
- **`get_annotation` 단건 조회 및 인젝션 도메인 개별 조작 명령 (`add_injection_domain`, `remove_injection_domain`)**: CLI에서 ID로 특정 단일 정책만 조회하는 명령과 스크립트 주입 도메인을 1-step으로 즉시 추가/삭제하는 명령어를 추가했습니다.
- **`cli init --check` 플래그 및 구버전 스킬 노티피케이션**: 스킬 설치/업데이트 상태를 미리 확인하는 `--check` 플래그를 추가하고, CLI 명령 실행 시 구버전 스킬이 감지되면 `stderr`로 안내 메시지를 출력합니다.
- **스킬 파일 자동 동기화 (`build.rs`)**: `cargo build` 시점에 개발 마스터 `SKILL.md`를 앱 내장 리소스 위치로 자동 복사하여 이중 관리 오차를 원천 차단했습니다.

### Changed (변경 사항)

- **CLI ↔ GUI 프록시 상태 파일 캐시 & 소켓 동기화 (`proxy_runtime.json`)**: GUI 앱에서 프록시 구동 시 런타임 포트와 PID를 상태 파일에 기록하고, CLI 조회 시 `load_active_state()` + TCP 소켓 핑을 거쳐 headless CLI 모드에서도 실제 켜져 있는 프록시 상태(`running: true`)를 정확히 인지하도록 개선했습니다.
- **CLI `add_annotation` / `update_annotation` 페이로드 대폭 간소화**: `id`, `timestamp`, `selector`, `domain`, `hostPattern`, `pathPattern`, `tagName`, `content`, `thumbnail`을 모두 옵셔널화하여 minimal 페이로드 작성을 지원합니다. `url` 기반 자동 패턴 도출 및 `locators` 기반 `selector` 자동 동기화가 적용됩니다.
- **`cli init` 스마트 자동 업데이트**: `cli init` 실행 시 `--force` 없이도 기존 스킬이 구버전이면 자동으로 최신 버전으로 갱신됩니다.

### Fixed (버그 수정)

- **CLI 모드 `Proxy stopped` 상태 불일치 해결**: GUI 프로세스와 CLI 프로세스 간 메모리 static 분리로 인해 CLI 명령에서 항상 `running: false`로 표시되던 이슈를 런타임 상태 파일 기반 검증으로 해결했습니다.
- **WinDivert 필터 예외 처리 및 무한 패킷 루프 방지**: `processId != {pid}` 필터를 적용하여 Horizon 자체 아웃바운드 패킷 재캡처(Self-Loop)를 방지하고 필터 호환성 예외 처리(Fallback)를 적용했습니다.
- **Windows CLI UTF-8 콘솔 입출력 보장**: Windows 터미널에서 CLI 실행 시 WinAPI `SetConsoleCP(65001)` 및 `SetConsoleOutputCP(65001)`을 적용했습니다.

## [v2.7.6] - 2026-08-11

### Added (추가 기능)

- **실시간 좌표 기반 배지 클러스터링 (`2+`)**: 동일 DOM 요소 및 화면 상의 픽셀 좌표 거리(X/Y ≤ 24px)를 실시간 감지하여 포개지는 배지들을 단일 스택 배지(`2+`)로 자동 결합합니다.
- **멀티 정책 팝업 탭 스위처**: 결합된 스택 배지 클릭 시 팝업 카드 최상단에 탭 바(`[#2 ...] [#3 ...]`)를 표시하여 한 화면에서 1초 만에 정책을 원클릭 교체 열람할 수 있습니다.
- **동적 라우트 & 와일드카드 패턴 매칭**: `pattern.ts`를 개편하여 Next.js/Nuxt 동적 파라미터(`[id]`, `{id}`, `:id`, `[...slug]`) 및 N단계 와일드카드(`/*`, `/**`)가 루트 페이지(`/`) 및 하위 모든 경로와 완벽 매칭되도록 확장했습니다.

### Changed (변경 사항)

- **정책 수정 모달 다크 글래스 테마 개편**: Watchtower 메인 앱의 정책 수정 모달(`src/routes/ux/policies/index.tsx`)을 인젝션 모달과 동일한 고급 다크 글래스 테마(`Edit3` 타이틀, `Globe`/`FolderTree` 아이콘, 핑크 그라데이션 저장 버튼)로 통일했습니다.
- **중첩 마크다운 리스트 들여쓰기 정돈**: 마크다운 가이드의 선두 공백/탭을 감지하여 번호 배지 아래 하위 불릿 항목(`• iframe...`)이 예쁘게 들여쓰기 렌더링되도록 개선했습니다.

### Fixed (버그 수정)

- **팝업 카드 화면 잘림 & 붕 뜨는 유격 제거**: 팝업 카드가 배지 도트 바로 위/아래 4px 지점에 밀착되도록 앵커링하고, 브라우저 사방 16px 안전 여백 및 동적 `maxHeight`를 적용해 화면 밖 잘림을 100% 차단했습니다.
- **스크롤 이동 후 스택 상시 유지**: `useCapture` 스크롤/리사이즈 이벤트를 바인딩하여 웹페이지 및 내부 scrollable 요소를 스크롤하는 중에도 겹침 스택이 실시간으로 상시 유지되도록 보안했습니다.

## [v2.7.5] - 2026-08-10

### Added (추가 기능)

- **어노테이션 다중 Locator**: 가이드가 CSS 단일 selector 대신 `testid` / `role` / `label` / `text` / `css` 우선순위 배열을 저장합니다. 클릭 캡처 시 목록을 만들고, 기존 데이터는 로드 시 마이그레이션합니다.
- **Locator 검증 & 승격**: 페이지 배지에 `ok` / `weak` / `broken` / `ambiguous`를 표시하고, weak이면서 fallback이 정확히 1개일 때만 primary 승격을 제안합니다(자동 rewrite 없음).
- **CLI ↔ GUI 어노테이션 동기화**: 실행 중인 앱이 `inspector_annotations.json` 변경을 감지해 `annotations-updated`를 발행하고, 인젝션 배지도 폴링으로 CLI 변경을 반영합니다.
- **CLI 가이드 문서**: Horizon Gateway skill에 어노테이션 CRUD, 쿼리 문법, validation 수리 규칙, `url` → `pathPattern` 자동 추출을 정리했습니다.

### Changed (변경 사항)

- **Policies & 인젝션 UX**: 가이드 Markdown 렌더링, 편집/신규 모달, host/path 패턴 헬퍼, Locator 작업용 배지·툴바 문구를 개선했습니다.

### Fixed (버그 수정)

- **`update_annotation` 부분 업데이트**: `hostPattern` / `pathPattern`을 생략해도 기존 값이 지워지지 않으며, `url`만 넘기면 path에서 `pathPattern`을 자동 추출합니다.

## [v2.7.4] - 2026-08-07

### Fixed (버그 수정)

- **인젝션 API 요청 폭주**: `useInjectionAppState` effect가 매 렌더마다 바뀌는 훅 반환 객체에 의존해 `status` / `proxy-routes` / `mock-rules` / `logging-domains`가 초당 수백 건씩 호출되던 무한 루프를 수정했습니다. 안정적인 fetch 콜백만 의존하도록 변경했습니다.

## [v2.7.3] - 2026-08-07

### Fixed (버그 수정)

- **TanStack Router 라우터 인버리언트 예외 수정**:
  - `usePanelNavigation`의 `useSearch` 및 `useNavigate`에 적용된 엄격한 경로 제약(`from: "/"`)을 유연하게 전환하여, 하위 라우트나 팝업/독립 창 화면에서 `CommandPalette`가 마운트될 때 발생하던 런타임 크래시(`Invariant failed: Could not find an active match from '/'`)를 해결했습니다.
- **테마 & 널 안전성 방어 로직 강화**:
  - `activeCustomThemeAtom` 및 `applyThemeToDocument`에 방어적 널 체크 및 기본값 Fallback 매핑을 추가하여 `localStorage` 테마 데이터가 유효하지 않거나 손상되었을 때 발생할 수 있는 런타임 렌더링 예외를 방지했습니다.
- **에러 바운더리 진단 기능 강화**:
  - `ErrorBoundary`에서 에러 스택, 컴포넌트 스택 추적 및 저장소 초기화(`Reset local storage`) 기능을 제공하도록 개선했습니다.

## [v2.7.2] - 2026-08-07

### Added (추가 기능)

- **`Ctrl+P` 커맨드 팔레트 & 정렬 규격**:
  - `[도메인]`, `[프록시]`, `[모킹]`, `[도구]`, `[로그]`, `[설정]`, `[팀]` 7개 영역으로 체계화된 24종 다국어 퍼지 검색 명령어 시스템을 구축했습니다.
  - 선택(`Select`), 자동완성(`Autocomplete`), 입력(`Input`) 다단계 실행 엔진 및 포커스 자동 관리.
- **커스텀 테마 & 폰트 에디터 (`chrome/theme`)**:
  - Primary, Secondary, Accent 배경 및 대치 글자색(Content Text Color) 10종 컬러 팔레트 그룹핑 및 내 컴퓨터 설치 폰트(`local()`) 커스텀 에디터를 구축했습니다.
  - `.hgtheme.json` 내보내기 및 불러오기 지원.

### Changed (변경 사항)

- **앱바(TopBar) Popover 메뉴 분리**:
  - 설정 아이콘 Popover 메뉴 내에 `🎨 테마 & 폰트 에디터` 독립 메뉴 항목을 배치했습니다.
- **프로필 UI 리팩토링**:
  - 아바타 테마 색상 스와치 및 어두운 프로필 이미지 배경을 제거하고, 이름·직무·언어 탭을 인라인 1행 구조로 전체 너비 확장했습니다.

### Fixed (버그 수정)

- **커맨드 팔레트 GPU 합성 병목 제거**:
  - 모달 전체 화면 `backdrop-blur-sm`을 제거하여 Windows WebView2 GPU 프레임 저하를 해소했습니다.
- **IPC 재호출 키보드 랙 방지**:
  - Autocomplete 단계에서 타이핑 시 매번 발생하던 백엔드 IPC 호출을 최초 1회 로드 후 인메모리 필터링으로 변경하여 60fps 즉시 반응 속도를 확보했습니다.
- **전역 ErrorBoundary 높이 확장**:
  - 렌더링 예외 복구 화면이 창 전체 높이(`min-h-screen`, `h-full`)를 채우도록 수직/수평 중앙 레이아웃을 수정했습니다.

## [v2.7.1] - 2026-08-07

### Added (추가 기능)

- **서버 리소스 Admin 편집**: Owner/Admin이 폼으로 워크스페이스 서버 항목(도메인·Mock·그룹·시나리오·그룹 연결)을 추가·수정·삭제할 수 있습니다. 전체 Push 없이도 가능합니다.

### Changed (변경 사항)

- **도메인 sync 라벨**: `http(s)://` 혼재를 없애 스캔하기 쉽게 통일했습니다.
- **공유 Modal**: radius를 `rounded-xl`로 줄이고, 패딩·헤더 클리핑을 고쳐 확인 다이얼로그가 잘리지 않게 했습니다.

### Fixed (버그 수정)

- **Mock 규칙 sync 목록**: host·name까지 매칭하고 동일 method/path 중복이 덮이지 않아, 서버 Mock이 Pull 목록에 모두 보입니다.

## [v2.7.0] - 2026-08-06

### Added (추가 기능)

- **팀 동기화 브라우저**: 종류별 카탈로그, Push/Pull 분리 diff, 시나리오 충돌 상세, 스냅샷 기반 diff(탭 전환마다 서버 재호출 없음).
- **워크스페이스 온보딩**: 워크스페이스가 없을 때 빈 목록 대신 가운데 생성·초대 코드 폼을 표시합니다.
- **Owner 워크스페이스 관리**: 이름 변경·삭제(이름 입력 확인), 멤버 제거, Owner 넘기기, Admin 부여/해제(Owner 전용 UI).
- **멤버 액션 확인 모달**: Admin 부여/해제 시 `window.confirm` 대신 전용 확인 다이얼로그를 사용합니다.

### Changed (변경 사항)

- **그룹 연결 sync 행**: 도메인 URL을 주 라벨로, 그룹명을 보조 텍스트로 표시해 스캔하기 쉽게 했습니다.
- **Admin 초대 권한**: 초대 코드·토큰 관리는 UI에서 Owner·Admin만 가능합니다.
- **도메인 sync 매칭**: 호스트 기준 매칭 시 `enabled`만 비교하고, URL 차이는 정보성 상세로 표시합니다.

### Fixed (버그 수정)

- **동기화 필터 선택**: 필터 전체 선택/해제가 필터된 목록에 올바르게 적용됩니다.
- **동기화 빈 상태**: 항목이 없을 때 안내 문구를 표시합니다.

## [v2.6.10] - 2026-08-06

### Added (추가 기능)

- **팀 워크스페이스 전체 뷰 UI**: DomainHub 스타일의 L→R 패널(워크스페이스 목록 · 홈 · 멤버 · 동기화 · 요금제)로 팀 화면을 재구성했습니다. `/team`과 Chrome 팀 서피스 진입점을 통일했습니다.

### Changed (변경 사항)

- **InjectionApp 모듈 분리**: ~3200줄 단일 파일을 `api/` · `hooks/` · `ui/` · `lib/`로 분리해 유지보수성을 개선했습니다.
- **인젝션 게이트 단순화**: 전역 Inspector On/Off와 무관하게, 등록 도메인 + `injection_domains` 목록만으로 HTML 주입 여부를 결정합니다. Inspector 설정 패널의 전역 토글과 `Ctrl+Alt+I` 단축키는 제거했습니다.
- **인젝션 UI 폴링 제거**: early interceptor가 fetch/XHR을 관찰하므로 `status` / `proxy-routes` / `logging-domains` 2.5초 하트비트 폴링을 없앴습니다. 팝오버를 열 때만 갱신합니다.

### Fixed (버그 수정)

- **XHR API 호출 누락**: early interceptor의 `XMLHttpRequest.send`가 `apply(this, body)`로 잘못 전달되어 POST/JSON 요청이 깨지던 문제를 `apply(this, arguments)`로 수정했습니다 (예: `GetContentAndTerms`).
- **fetch 이중 패치 제거**: InjectionApp이 fetch/XHR을 다시 감싸던 코드를 제거해 일부 API가 차단되던 부작용을 없앴습니다.

## [v2.6.9] - 2026-08-06

### Added (추가 기능)

- **선택적 워크스페이스 동기화 모달**: 팀 도메인 push/pull 시 매칭 키, 겹침 정책, 종류 필터, 도메인별 선택을 지원합니다.
- **툴바 업데이트 배지**: 업데이트가 있으면 타이틀바에 표시되고, 클릭 시 설치합니다.
- **팀 멤버 신원 표시 개선**: display name 우선·이메일은 보조로 표시, 로그인 시 표시 이름 편집, 워크스페이스 동료 프로필 조회.

### Fixed (버그 수정)

- **inspector.js stub 서빙**: Tauri 리소스 번들·다경로 해석·빌드 타임 임베드로 `/.horizon-gateway/inspector.js`가 “pnpm build:injection” 경고 stub만 내려주던 문제를 수정했습니다.
- **HTML 인젝션 누락**: `</body>`가 없으면 `</html>`/문서 끝에 inspector 스크립트를 넣고, early interceptor만 있던 부분 주입도 보완합니다.
- **도메인 중복/동기화 hostname 매칭**: scheme·path·port 변형을 같은 호스트로 인식하도록 hostname 기준으로 정규화·매칭합니다.

## [v2.6.8] - 2026-08-06

### Added (추가 기능)

- **중복 도메인 감지 및 병합 정책 모달**: 로컬 목록과 팀 워크스페이스 간 중복 도메인 URL을 자동 감지합니다. 경고 배너를 표시하고, Proxy 스타일의 병합 정책 모달에서 3가지 통합 전략(`Smart Merge`, `Keep Latest`, `Keep Oldest`)을 제공합니다.
- **도메인 설정 비교 뷰**: 중복 병합 모달 내 도메인 ID별 2열 비교 카드 뷰를 추가했습니다. 그룹 배정, 프록시 대상, 모니터 상태, API 로깅 배지를 나란히 표시해 주 도메인 ID를 선택할 때 참고할 수 있습니다.
- **멀티모드 워크스페이스 도메인 동기화**: 팀 워크스페이스 동기화 모달에 4가지 동기화 전략 모드(`URL 기반 병합`, `추가만`, `전체 덮어쓰기`, `엄격한 내부 ID`)를 추가하여, 계정·기기 간 ID 충돌 없이 도메인 목록을 동기화할 수 있습니다.

### Changed (변경 사항)

- **UI 다듬기 및 모던한 Border Radius**: 모달·카드 전반의 border-radius를 `rounded-3xl`/`rounded-2xl`에서 더 날렵한 `rounded-xl`/`rounded-lg`/`rounded-md`로 조정해 프리미엄 데스크톱 UI에 맞게 다듬었습니다.
- **100% SVG 벡터 아이콘**: 일반 텍스트 이모지 아이콘(`🌐`, `➕`, `⚠️`, `🆔`, `⚡`)을 Lucide SVG 아이콘(`Globe`, `Plus`, `AlertTriangle`, `Fingerprint`, `Zap`, `Server`, `Shield`)으로 교체했습니다.

## [v2.6.7] - 2026-08-06

### Added (추가 기능)

- **실시간 워크스페이스 초대 알림 & 1-Click 수락 뷰**: Supabase Realtime 이벤트를 연결하여 로그인 중 새로운 초대를 받으면 즉시 알림 토스트가 뜹니다. "나에게 온 초대 목록" 카드에서 36자리 토큰 입력 없이 `[수락]` / `[거절]` 1-Click 버튼으로 팀에 참가할 수 있습니다.
- **공유용 공개 초대 토큰 생성**: 이메일을 지정하지 않고 누구나 해당 링크/토큰으로 참가할 수 있는 공개 초대 토큰(`공유 토큰 생성`) 발급 기능을 추가했습니다.
- **초대 토큰 수동 만료/철회 UI**: 대기 중인 초대 목록에 `[만료/철회]` 버튼을 제공하여 필요 없는 공개/이메일 초대 토큰을 수동으로 비활성화할 수 있습니다.

### Fixed (버그 수정)

- **Supabase 초대 RLS 권한 정책 수정**: 피초대자가 초대 토큰 직접 입력 또는 공유 링크로 참가 시 403 Forbidden 및 Row Not Found가 발생하던 RLS 조회/업데이트 정책을 수정하여 `status = 'pending'` 조건에서 토큰 조회가 가능하도록 허용했습니다.
- **워크스페이스 관리자 권한 함수 개선**: `is_workspace_admin()` 함수에 `workspaces.owner_id = auth.uid()` 조건을 추가하여 워크스페이스 소유자가 멤버 행 업서트 전에도 정상적으로 초대를 생성할 수 있도록 조치했습니다.

## [v2.6.6] - 2026-08-06

### Added (추가 기능)

- **팀 워크스페이스 도메인 리스트 동기화 (1차)**: 명시적 Push/Pull 방식을 통해 팀원 간 도메인 목록, 그룹, Mock 규칙을 동기화하는 기능을 추가했습니다. CA 인증서, 토큰, 패킷 로그 등 민감 정보는 제외하고 설정 데이터만 안전하게 공유합니다.
- **Free Tier 정책 및 워크스페이스 게이트키핑**: Free Tier 사용자의 워크스페이스 1개 소유 제한 및 정원(최대 3명) 제한을 관리하는 `useWorkspaceGuard`를 도입했습니다. 정원 초과 또는 결제 만료(`past_due`/`canceled`) 시 안내 배너를 노출하고 동기화 기능을 제한합니다.
- **대기 중인 초대 토큰 관리 및 즉시 복사**: 멤버 초대 시 생성된 초대 토큰(UUID)을 클립보드에 자동 복사하고, "대기 중인 초대" 목록에서 언제든 토큰을 다시 복사할 수 있는 1-Click 복사 버튼을 추가했습니다.
- **소셜 공유 OG 메타 태그**: 공식 웹사이트 랜딩·Changelog 페이지에 Open Graph·Twitter Card 메타 태그를 추가했습니다. 카카오, Microsoft Teams, Slack, LinkedIn 등에서 `https://gateway.delete-horizon.com/ko/changelog/` 링크 공유 시 제목·설명·이미지가 포함된 카드 미리보기가 표시됩니다.
- **OG 공유 이미지**: Horizon Gateway 로고 기반 1200×630 PNG OG 이미지(`og-image.png`)를 추가하고, 빌드 시 SVG 소스에서 자동 생성하는 스크립트를 웹사이트 빌드 파이프라인에 포함했습니다.

### Changed (변경 사항)

- **워크스페이스 동기화 UX 개선**: `TeamSection` 내의 불필요한 동기화 토글 스위치를 제거했습니다. 워크스페이스 선택 시 `도메인 목록 업로드 (Push)` 및 `가져오기 (Pull)` 버튼이 항상 즉시 노출됩니다.
- **웹사이트 canonical·OG URL 정비**: Astro `site`를 `https://gateway.delete-horizon.com`으로 설정하고 `canonical`, `og:url`, `og:site_name`, `og:locale`, 이미지 크기·alt 메타 태그를 추가했습니다.
- **Changelog 페이지 공유 설명**: Changelog URL 공유 시 랜딩 페이지 문구 대신 changelog 전용 description(`Horizon Gateway의 최신 기능 추가 및 업데이트 이력입니다.`)이 카드에 표시됩니다.

### Fixed (버그 수정)

- **Supabase 워크스페이스 RLS 정책 보완**: `is_workspace_member()` 헬퍼 함수와 워크스페이스 SELECT 정책을 수정하여, 소유자(`owner_id = auth.uid()`)가 멤버 행 삽입 전에도 본인의 워크스페이스 및 리소스를 403 / 42501 RLS 거부 없이 조회 및 업로드할 수 있도록 보완했습니다.
- **프로덕션 빌드 inspector.js 제공 수정**: 릴리스 빌드에서 `/.horizon-gateway/inspector.js`가 번들 리소스를 찾지 못해 경고 스텁만 제공되던 문제를 수정했습니다. Tauri `resource_dir()`로 `inspector.js` 경로를 해석하여 개발·프로덕션 환경 모두에서 인젝션 스크립트가 올바르게 제공됩니다.

## [v2.6.5] - 2026-08-05

### Added (추가 기능)

- **선언된 API만 트래픽 로깅**: 인젝션 인스펙터가 Horizon Gateway의 API 로깅 설정에서 명시적으로 선언된 도메인의 트래픽만 LOG 탭에 기록하도록 변경했습니다. CDN, 분석, 미디어 등 무관한 3rd-party 트래픽은 자동으로 무시됩니다.
- **오프라인 프록시 폴백 에러 페이지**: 로컬 프록시 대상(예: `localhost:3000`)에 연결할 수 없을 때, 브라우저에 스타일된 에러 페이지를 표시하면서 Horizon Gateway 인젝션 UI도 함께 노출합니다. 빈 화면에 갇히지 않고 에러 페이지에서 프록시 라우트를 직접 끄고 켤 수 있습니다.

### Changed (변경 사항)

- **모킹 API 상세 및 편집 화면 통합**: 읽기 전용 상세 뷰와 편집 폼을 하나의 모달로 합쳤습니다. 신규 작성 시 `"신규 모킹 규칙 작성"`, 기존 편집 시 `"모킹 API 상세 및 편집"`으로 타이틀이 동적으로 변경됩니다.
- **모킹 목록 SVG 아이콘 정리**: MCK 팝오버 및 모달의 모든 이모지 텍스트 아이콘(`⚡`, `➕`, `✏️`, `🗑️`, `👁️`)을 깔끔한 SVG 아이콘으로 교체했습니다.
- **PRX 팝오버 정확한 호스트 매칭**: 인젝션 PRX 팝오버의 도메인 라우트 매칭을 `includes` / `endsWith` 방식에서 정확한 일치(`hostname === domain`)로 변경했습니다. 현재 호스트와 무관한 서브도메인의 라우트가 목록에 표시되던 문제를 수정했습니다.
- **에러 경계 브랜딩**: 인젝션 스크립트 에러 메시지를 `Watchtower Error`에서 `Horizon Gateway Error`로 변경했습니다.

### Fixed (버그 수정)

- **모킹 ON/OFF 토글 동작 수정**: `handleToggleMockRule`이 저장된 백엔드 `MockRule`(`/api/mock-rule/toggle` 사용)과 미저장 `MockedApiEntry` 캡처 항목(`/api/mock-rule/save`로 자동 저장 후 활성화) 모두 올바르게 처리하도록 수정했습니다.
- **트래픽 로그 stale 클로저 버그 수정**: `fetch`/`XHR` 패치 `useEffect` 안의 `logTraffic`이 마운트 시점에 빈 `loggingDomains` 배열을 캡처하여 이후 도메인이 추가돼도 필터가 무효화되던 React 클로저 버그를 수정했습니다. `loggingDomainsRef`를 도입해 항상 최신 도메인 목록을 참조하도록 변경했습니다.
- **early interceptor 로그 재동기화**: `loggingDomains` API 응답이 도착하면 early interceptor의 `__wt_api_traffic_logs` 버퍼를 다시 sync하는 `useEffect`를 추가했습니다. React 마운트 이전에 캡처된 로그도 올바르게 복원됩니다.

## [v2.6.4] - 2026-08-04



### Added (추가 기능)

- **API 본문 저장 (Body Logging) 옵션 복원**: API 로깅 패널 상단 툴바 및 전체 화면 스키마 옵션 페이지(`/apis/schema`)에 요청/응답 본문(Body) 저장 켜고 끄기 토글 스위치 옵션을 복원하여 자유롭게 설정할 수 있도록 개선했습니다.

## [v2.6.3] - 2026-08-04

### Added (추가 기능)

- **OpenAPI 스키마 관리 및 다운로드 UI 복원**: 도메인 개요 패널(`api/schema`) 및 글로벌 스키마 탐색기 화면(`/apis/schema`)에 OpenAPI 스키마 URL 등록 및 실시간 다운로드 폼을 추가했습니다. 스키마가 없는 도메인을 선택한 경우에도 에러 카드에서 즉시 스키마 URL을 저장하고 다운로드할 수 있습니다.

### Changed (변경 사항)

- **도메인 개요 패널 API 메뉴 레이아웃 통일**: `DomainOverviewPanel`의 `API 로그`, `API 스키마`, `모킹` 메뉴의 상하 간격과 왼쪽 들여쓰기를 통일하여 정돈된 UI 레이아웃으로 변경했습니다.
- **글로벌 스키마 탐색기 도메인 선택 및 패딩 개선**: 도메인 선택 드롭다운에서 모든 등록된 API 도메인을 탐색할 수 있도록 확장하고, 오버레이 서피스 창의 테두리 여백(`p-3 sm:p-4` / `px-4 pb-4`)을 추가하여 화면에 바짝 붙지 않도록 개선했습니다.

## [v2.6.2] - 2026-08-04

### Added (추가 기능)

- **프록시 / 모킹 / 로깅 연동 자동 스크립트 인젝션**: 프록시, 모킹, 로깅 중 하나라도 활성화되어 있으면 인스펙터 스위치를 별도로 켜지 않아도 타겟 앱에 `inspector.js` 스크립트와 상태바 위젯이 자동으로 수집 및 표시되도록 개선했습니다.

### Changed (변경 사항)

- **모킹 활성화 시 HTTPS 선택적 복호화**: HTTPS CONNECT 터널링 시 모킹 및 로깅이 켜진 도메인에 대해서도 복호화(`should_decrypt`)가 이루어지도록 확장하여 HTTPS 웹사이트에서도 인젝션 상태바 및 모킹 규칙이 제대로 적용되도록 수정했습니다.
- **정확한 프록시 상태 감지**: `/.horizon-gateway/api/status`에서 하드코딩된 `proxy: true`를 제거하고, 실제 로컬 라우팅 활성화 상태 및 활성화된 라우트 개수(`proxyCount`)를 반영하여 잘못된 프록시 활성화 표시 버그를 수정했습니다.
- **인젝션 상태바 브랜딩 및 UX 개선**: 단순 텍스트("W" / "⋮") 모드 체인지를 공식 Watchtower SVG 로고(`/.horizon-gateway/logo.svg`)로 대체하고, 인스펙트 모드 시 은은한 광원(Glow) 효과를 적용했습니다.

## [v2.6.1] - 2026-08-01

### Added (추가 기능)

- **API 로그 본문 검색**: 일별 SQLite FTS5 사이드카로 요청/응답 본문을 자유 검색하며, 로그 UI에는 검색 히트를 점진적으로 전달합니다.
- **적응형 파라미터 인덱싱**: `key=value` 본문 스캔으로 아직 학습되지 않은 키를 `indexed_params.json`에 등록하고 파라미터 인덱스를 백필해 이후 조회를 빠르게 합니다.

### Changed (변경 사항)

- **API 로그 저장 분리**: 일별 저장을 경량 메타(`.meta.jsonl`)와 항목별 본문 파일로 나누고, 목록/상세 API와 레거시 `.jsonl` 이중 읽기를 지원합니다.
- **API 로그 목록 UX**: 도메인·오버뷰 로그 패널이 폴링 대신 `api-log-captured` 이벤트로 갱신되며, 행 클릭 시 상세를 필요할 때만 로드합니다.
- **프록시 연결 맵 필터**: 그룹 칩, 라우트 상태(활성/비활성/미연결) 필터, 검색어 유지를 프록시 그래프 화면에 추가했습니다.
- **도메인 API 로그 탐색**: 오버뷰·API 패널의 "API 로그"가 전체 화면이 아니라 우측 패널 컬럼으로 열리도록 변경했습니다.

### Performance (성능 개선)

- **오늘 메타 캐시·쓰기 경로**: 오늘 요약 인메모리 캐시, 날짜별 쓰기 락, 본문 크기 상한을 적용하고, 로깅이 꺼진 패스스루 트래픽은 디스크에 쓰지 않습니다.

## [v2.6.0] - 2026-07-23

### Added (추가 기능)

- **내부 API 요청 로깅**: API 스키마 Try-it-out (`send_api_request`) 및 파이프라인 API 노드 등의 내부 테스트 요청 시, 프록시가 활성화되어 있고 해당 도메인의 API 로깅 기능이 켜져 있으면 네트워크 로그가 자동으로 수집/기록되도록 개선했습니다.

### Performance (성능 개선)

- **API 로그 상세 조회 최적화**: `fetchApiLogById`의 날짜별 로그 파일 탐색 방식을 `Promise.all` 병렬 처리로 변경하여 상세 창 조회 시 로딩 속도를 최적화했습니다.

## [v2.5.4] - 2026-07-16

### Changed (변경 사항)

- **그룹 카드 그리드**: 고정 2열 레이아웃을 카드 최소 너비 320px 기준 `auto-fill` 반응형 컬럼으로 바꿔, 오버레이·팝업 패널 너비에 맞게 그룹 카드가 배치되도록 했습니다.

### Fixed (버그 수정)

- **그룹 관리 목록 로딩**: `useDomainHubData()` 호출마다 따로 관리되던 `loading` 상태를 공유해, 메인 허브에서 이미 그룹 데이터를 불러온 뒤 그룹 오버레이를 열어도 무한 스피너가 뜨지 않도록 수정했습니다.

## [v2.5.3] - 2026-07-14

### Changed (변경 사항)

- **Fluent 오버레이 스크롤바**: 메인·보조 창이 WebView2 `fluentOverlay` 스크롤바를 사용해 목록·패널에 클래식 ↑↓ 버튼 없이 스크롤됩니다.
- **테마 연동 스크롤바 폴리싱**: 앱 전역 thin pill thumb가 라이트/다크 `base-content`를 따릅니다. Chromium `scrollbar-width`는 Windows fluent 화살표를 강제해 사용하지 않습니다.

### Fixed (버그 수정)

- **Windows GUI 콘솔 창**: 릴리스 빌드에서 windows 서브시스템을 사용해 탐색기/시작 메뉴로 실행할 때 CMD 로그 창이 뜨지 않도록 했습니다. CLI 모드는 stdout/stderr가 파이프·파일로 리다이렉트되지 않은 경우에만 부모 콘솔에 연결해, 에이전트 pipe 캡처는 그대로 유지합니다.

## [v2.5.2] - 2026-07-14

### Added (추가 기능)

- **다국어 및 확장 가능한 업데이트 안내 모달**: 앱 업데이트 완료 후 최초 진입 시 무엇이 새롭게 추가되었는지 한눈에 보여주는 고급스러운 디자인의 업데이트 알림 모달을 도입했습니다.
- **상단 설정 메뉴 연동**: 언제든지 과거 업데이트 기록을 찾아볼 수 있도록 상단 바 설정 드롭다운 메뉴에 "업데이트 내역" 옵션을 신설하고 연동했습니다.
- **마크다운 동적 파싱 및 가상화 스크롤**: 컴파일 타임에 마크다운 소스(`CHANGELOG.md` / `CHANGELOG.ko.md`)를 통째로 읽어 동적으로 파싱함으로써 단일 소스로 데이터의 정합성을 보장하며, 전체 이력 조회 시에도 렉이 없는 초고속 가상화 스크롤을 구현했습니다.
- **한글 변경 이력 전체 번역본 배포**: 웹사이트와 앱 모달 모두에서 완벽한 다국어 조회가 가능하도록 프로젝트 루트에 `v1.0.0`부터의 역사를 온전히 한글로 번역한 `CHANGELOG.ko.md`를 배포했습니다.

## [v2.5.1] - 2026-07-14

### Changed (변경 사항)

- **앱 리브랜딩 마침**: `watchtower`에서 `horizon-gateway`로 남아있던 파일, 내부 변수, 디렉토리 및 셀렉터를 모두 마이그레이션했습니다.
- **안정적인 데이터 마이그레이션**: 앱을 업그레이드할 때 도메인 목록이나 설정이 유실되지 않도록, 기존의 사용자 데이터베이스(예: `domains.json`)가 존재하는지 체크하여 자동으로 설정 파일을 복사 및 마이그레이션해 주는 로직을 추가했습니다.

## [v2.5.0] - 2026-07-13

### Added (추가 기능)

- **Supabase 인증 및 데이터베이스 통합**: Supabase JS SDK를 앱에 연동했습니다. GitHub을 통한 소셜 OAuth 로그인을 지원합니다.
- **Tauri v2 딥링크 라우팅**: 앱 시작 시 Windows 레지스트리에 자동 등록되는 커스텀 URL 스킴 핸들러(`horizon-gateway://`)를 활성화했습니다.
- **Windows 단일 인스턴스 실행 및 인자 전달**: 여러 개의 창이 동시에 뜨는 것을 방지하기 위해 `tauri-plugin-single-instance`를 추가했습니다. 실행 중인 메인 창으로 OAuth 콜백 URL 등의 매개변수가 누락 없이 전달되도록 CLI 인자 수동 파싱 및 라우팅 로직을 구현했습니다.
- **GitHub Sponsors 연동**: 국내 결제 서비스 제한을 방지하기 위해 개발자 후원 플랫폼을 GitHub Sponsors로 전환했습니다. 테마에 맞게 깜빡이는 로즈(Rose) 후원 단추가 추가되었습니다.
- **얼리 액세스 연구소 및 개발자 피드백**: 후원자용 실험적 기능 설정 옵션과 Supabase DB에 직접 의견을 제출하는 개발자 피드백 폼을 사용자 설정 페이지에 추가했습니다.
- **설정 및 프로필 드롭다운 정리**: TopBar 버튼들을 재조직하여, 인프라 설정 버튼을 클릭형 설정 드롭다운 메뉴로 통합하고 프로필 관리 및 로그아웃 버튼을 하나의 깔끔한 프로필 드롭다운 메뉴로 합쳤습니다.
- **상단 바 Chevron 아이콘 제거**: 더 간결하고 일관된 레이아웃을 위해 상단 바 메뉴 단추에서 쉐브론 아이콘을 제거했습니다.
- **자동 GitHub 프로필 동기화**: OAuth 로그인을 수행할 시 사용자 메타데이터를 기반으로 프로필 이름과 아바타 이미지를 자동으로 로컬 프로필과 동기화합니다.

### Changed (변경 사항)

- **앱 리브랜딩**: 기존 `watchtower`에서 `horizon-gateway`로의 이름 및 설정 마이그레이션을 계속 진행했습니다.
- **Tauri Opener 우회**: Tauri Opener 라이브러리의 보안 제약을 우회하여 외부 브라우저 URL을 열 수 있도록 커스텀 Rust 백채널 `open_external_url` 명령을 통해 열리도록 개선했습니다.

## [v2.4.4] - 2026-07-10

### Added (추가 기능)

- **API 로그 다중 HTML 내보내기**: 여러 개의 API 로그 항목을 선택하여 하나의 통합 HTML 보고서(목차, 항목별 복사 액션, 본문 스크롤 지원 등 포함)로 내보낼 수 있는 기능이 추가되었습니다.
- **저장 및 폴더 열기**: HTML 내보내기 시 기본 저장 창(Tauri 파일 다이얼로그)을 사용하고, 성공적으로 저장 완료 시 `revealItemInDir`를 통해 "폴더 열기" 액션을 제안하도록 구현했습니다.
- **공통 다운로드 헬퍼**: 재사용 가능한 파일 다운로드 흐름을 제공하기 위해 `saveTextDownload`, `revealInFolder`, `offerRevealSavedDownload` 함수를 추가했습니다.

### Changed (변경 사항)

- **HTTP 본문 표시**: `formatHttpBody` 함수가 이스케이프 문자(`\n`, `\t`)를 원본 그대로 처리하며, 이중 인코딩된 JSON 문자열을 보기 쉽게 풀어내어 복사/추출 시 실제 데이터와 더 가깝게 가독성을 개선했습니다.
- **HTML 카드 이스케이프**: 복사 또는 내보내기용 HTML 카드 구성 시 발생 가능한 보안 이슈를 막기 위해 `<pre>` 태그 및 헤더 내 사용자 콘텐츠를 적절히 이스케이프하도록 수정했습니다.

## [v2.4.3] - 2026-07-10

### Added (추가 기능)

- **DisabledPanel (비활성 패널)**: 도메인에서 사용할 수 없는 기능 패널 진입 시, 조용히 Overview로 리다이렉트되는 대신 비활성화 상태임을 사용자에게 명시적으로 안내하는 패널을 구현했습니다. 사용자는 이 화면에서 직접 클릭하여 즉시 기능을 켤 수 있습니다.
- **도메인 주소 일괄 복사**: 새 대량 처리 툴바(`DomainListBulkToolbar`)에 선택한 모든 도메인의 URL 주소를 순서대로 클립보드에 한번에 복사하는 "URL 복사" 액션을 도입했습니다.
- **범위 선택 및 개별 토글 선택**: 도메인 대량 관리 모드에서 Shift+클릭을 통한 범위 선택과 Ctrl+클릭을 통한 개별 토글이 가능하도록 사용성을 향상하고 시각적 안내를 추가했습니다.
- **`HubSurfaceEmbedContext` 닫기 콜백**: `HubSurfaceEmbedProvider`에 `onDismiss` 프로퍼티를 추가하고 `useHubSurfaceDismiss()` 훅을 제공합니다. 이를 통해 모달 내 임베디드 오버레이(예: 도메인 추가 등)를 닫을 때 Tauri 창 전체가 닫히지 않고 내부 오버레이만 올바르게 닫힙니다.
- **다국어(i18n)**: 한글 및 영어 다국어 번들 파일에 `bulkCopyUrls`, `bulkCopied`, `bulkSelectionHint` 등의 문구를 추가했습니다.

### Changed (변경 사항)

- **도메인 변경 시 패널 깊이(Depth) 보존**: 도메인을 전환하더라도 이전에 보던 패널 깊이를 초기화하여 `overview`로 돌리는 대신 동일한 패널 상태를 유지합니다.
- **대량 관리 상태 비저장**: 사용자가 페이지를 새로고침할 시 대량 선택 상태가 남아 꼬이는 현상을 방지하기 위해 `domainListBulkModeAtom`을 일반 `atom`으로 변경했습니다.
- **대량 선택 타입 최적화**: 선택 여부를 확인하는 속도를 O(1) 수준으로 빠르게 유지하기 위해 `domainListBulkSelectedIdsAtom` 형식을 `ReadonlySet<number>`로 바꿨습니다.
- **도메인 리스트 리팩토링**: 코드 가독성과 유지보수성 향상을 위해 `DomainListPanel`에서 `DomainListItem`, `VirtualizedGroupedDomainList`, `DomainListInteractionContext`를 별도 파일로 추출했습니다.
- **`applyNavigation` 헬퍼**: URL을 네비게이션하기 전에 내부 참조값(ref)과 Jotai 상태가 완전하게 일치하도록 동기화해 주어, 비동기 레이스로 인해 두 번 페이지 이동이 일어나던 이슈를 방지합니다.

### Fixed (버그 수정)

- **도메인 선택 해제 시 참조 레이스 해결**: `selectDomain`, `clearDomain`, `restoreNavigation` 함수들이 `applyNavigation`을 사용하여 URL 네비게이션 전에 내부 참조값(`domainIdRef`/`panelsRef`)을 정확히 갱신하도록 바로잡았습니다.
- **불필요한 패널 리셋 제거**: 특정 패널에 접근 불가할 때 무조건 `overview`로 리셋하던 `useEffect` 문을 제거하고, 대신 `DisabledPanel`을 렌더링하도록 개선했습니다.

## [v2.4.2] - 2026-07-09

### Fixed (버그 수정)

- **Windows CLI 파이프 출력 해결**: release 빌드 시 `AttachConsole` 호출 후 stdio 재바인딩 로직을 개선하여, PowerShell 및 cmd 환경에서 파이프 전송과 출력이 정상적으로 출력되도록 수정했습니다.
- **GUI 실행 시 콘솔 창 깜빡임 현상 수정**: CLI 가 아닌 GUI 모드로 실행할 시 뒤편의 콘솔 창이 노출되지 않고 바로 숨겨지도록 처리했습니다.

## [v2.4.1] - 2026-07-09

### Fixed (버그 수정)

- **Windows CLI 콘솔 출력 안정화**: standard 입출력 장치 바인딩을 조정해 PowerShell에서도 JSON 결과물이 지연 없이 잘 찍히도록 수정했습니다.

## [v2.4.0] - 2026-07-09

### Added (추가 기능)

- **프록시 연결 그래프 뷰**: 도메인과 로컬 프록시 대상 간의 연결 상태를 Bezier 곡선과 흐르는 트래픽 애니메이션으로 시각화해 주는 인터랙티브 그래프 뷰를 추가했습니다.
- **도메인 리스트 가상화**: 수백 개의 도메인을 빠르게 스크롤할 수 있도록 도메인 목록 렌더링에 가상화를 도입했습니다.
- **스마트 정렬 순서 도입**: 프록시 경로가 켜져 있는 도메인을 우선하여 상단에 보여주는 스마트 정렬 기능을 적용했습니다.
- **헤드리스 CLI 기능**: Tauri/WebView를 실행하지 않고 커맨드라인에서 바로 백엔드 데이터 처리가 가능한 `watchtower cli run` 모드를 추가했습니다.

## [v2.3.1] - 2026-07-09

### Added (추가 기능)

- **샌드박스 라이브러리 CLI 지원**: 파이프라인 데이터, JSON 스키마, 크립토 설정을 파일 기반 영구 저장소에 기록 및 CRUD할 수 있도록 백엔드 CLI 기능을 연동했습니다.
- **CLI 명령어 카테고리 태깅**: GUI 전용 명령어를 에이전트가 구분하여 필터링할 수 있도록 명령어 메타데이터 정보를 추가했습니다.

## [v2.3.0] - 2026-07-08

### Added (추가 기능)

- **허브 핸드오프 및 오버레이 서피스**: API 로그 디테일 패널에서 모킹 설정, 스키마 탐색기, 크립토 도구 등으로 컨텍스트 정보를 넘겨주며 페이지를 전환하는 유기적 핸드오프 플로우를 적용했습니다.
- **글로벌 스키마 탐색기**: 전달받은 OpenAPI 스펙 메타데이터 정보(메서드/경로)를 기반으로 해당 스키마 엔드포인트를 사전 선택해 띄워 주는 글로벌 탐색 서피스를 구현했습니다.

## [v2.2.0] - 2026-07-08

### Added (추가 기능)

- **도메인 허브**: 기존의 사이드바/대시보드 메인을 대체하여 패널 스택 이동, 도메인 상세 정보, 프록시/API 모니터링 토글을 지원하는 통합 도메인 허브 화면을 설계했습니다.
- **팝업 윈도우 지원**: 인프라, 설정, 프로필 등의 주요 기능들을 별도의 독립 창으로 분리해 띄울 수 있는 독립 팝업 뷰 라우트를 구현했습니다.

## [v2.1.4] - 2026-07-07

### Fixed (버그 수정)

- **CLI 무한 대기 버그 수정**: GUI 미사용 단독 명령어 실행 시 Tauri 전체 부트스트랩을 우회하여 명령 처리가 지연 없이 즉시 종료되도록 수정했습니다.

## [v2.1.3] - 2026-07-07

### Added (추가 기능)

- **브랜드 로고 통합**: 심플하고 대칭성 있는 신규 브랜드 타이틀 로고를 도입하고 자동 로고 동기화 스크립트(`sync-logos.mjs`)를 구축했습니다.

## [v2.1.2] - 2026-07-07

### Added (추가 기능)

- **Windows CLI 콘솔 어태치**: 릴리스 빌드에서도 명령줄 호출 시 터미널 상에 Rust `println!` 출력이 잘 보이도록 부모 프로세스 콘솔에 바인딩되도록 개선했습니다.

## [v2.1.1] - 2026-07-07

### Added (추가 기능)

- **설치 시 PATH 환경 변수 추가**: Windows NSIS 인스턴스 설치 시 사용자의 선택에 따라 설치 경로를 시스템 환경 변수에 자동으로 등록해 주어 전역 실행이 가능하게 했습니다.

## [v2.1.0] - 2026-07-06

### Added (추가 기능)

- **헤드리스 CLI 모드**: GUI 없이 터미널에서 JSON 데이터 조회 및 조작이 가능한 CLI 명령어 셋을 릴리스했습니다. 에이전트용 스킬 파일 배포 명령어도 함께 제공합니다.

## [v2.0.1] - 2026-07-06

### Changed (변경 사항)

- **클라이언트 사이드 API 로그 필터링**: 빈번한 네트워크 요청을 줄이고 화면 반응 속도를 개선하기 위해, 입력창 입력 시마다 서버에 쿼리하는 대신 클라이언트에서 로그 데이터를 불러와 메모이제이션 기반 필터링을 수행하도록 최적화했습니다.

## [v2.0.0] - 2026-06-28

### Added (추가 기능)

- **샌드박스 데이터 파싱 모나코 에디터 탑재**: 텍스트 에어리어 입력 창을 고성능 Monaco Editor 기반 `TsCodeEditor`로 대체하여 하이라이팅, 자동완성, 스키마 유효성 검증을 지원합니다.
- **동적 실시간 렌더링 캔버스 노드**: 작성한 React/JSX 코드를 가상 아이프레임 내에서 실시간 트랜스파일하여 바로 화면에 렌더링해 주는 미리보기 노드 기능을 탑재했습니다.

## [v1.8.1] - 2026-06-11

### Fixed (버그 수정)

- **Next.js HMR 웹소켓 프록시 루프 수정**: Next.js 핫 리로딩 도중 웹소켓 업그레이드 요청이 막히던 현상을 해결하기 위해, HTTP/1.1 클라이언트 핸드셰이크 프로토콜 및 커스텀 헤더 재작성 로직을 프록시에 적용했습니다.

## [v1.8.0] - 2026-06-10

### Added (추가 기능)

- **안드로이드 USB 연결 탭**: Tailscale VPN 무선 연결 외에 ADB를 활용한 유선 디버깅 기능 탭을 모바일 설정에 신설했습니다.
- **안드로이드 시스템 프록시 자동 설정**: USB 연결 활성화 시, 개발자가 안드로이드 와이파이 프록시 설정을 수동으로 치지 않아도 ADB 커맨드를 통해 기기의 시스템 프록시 환경 변수를 자동으로 주입 및 삭제해 주도록 자동화했습니다.

## [v1.7.6] - 2026-06-08

### Added (추가 기능)

- **API 로그 다중 복사 기능**: 로그 상세 정보 복사 단추를 확장하여 Azure DevOps용 HTML 복사 및 팀즈/마크다운에 적합한 클립보드 내보내기 포맷 형식을 선별 지원합니다.

## [v1.7.5] - 2026-06-08

### Added (추가 기능)

- **Tauri Specta v2 전환**: 수작업으로 스키마 타입을 연동하던 기존 방식을 버리고, Rust 백엔드 명령 구조를 Specta 기반 타입스크립트 인터페이스 바인딩 파일(`bindings.ts`)로 자동 생성되게 개선했습니다.

## [v1.7.4] - 2026-04-29

### Fixed (버그 수정)

- **API 모의 규칙 데이터 매핑 동기화**: 백엔드와 프론트엔드 간의 모의 규칙 API 요청 필드 규격 불일치로 규칙 등록이 실패하던 문제를 구조적 연동 최적화로 해결했습니다.

## [v1.7.3] - 2026-04-16

### Added (추가 기능)

- **사이드바 서비스 실시간 모니터링**: 프록시, 모킹, 인젝션 엔진 등 주요 기능들의 가동 상태를 사이드바 메뉴 옆의 깜빡이는 실시간 LED 상태 인디케이터로 파악 가능하게 개선했습니다.

## [v1.7.2] - 2026-04-14

### Added (추가 기능)

- **정책 리스트 이중 뷰 모드**: 정책 관리의 편의를 위해 카드식으로 편집하는 편집 모드(Manage Mode)와 최종 문서 서식 미리보기 및 PDF 출력을 지원하는 보고서 모드(Report Mode)를 도입했습니다.

## [v1.7.1] - 2026-04-13

### Fixed (버그 수정)

- **정적 코드 검사 정리**: Biome 린터에서 경고하는 미사용 변수, 잘못 차단된 중복 함수 정의 등을 리팩토링했습니다.

## [v1.7.0] - 2026-04-13

### Added (추가 기능)

- **스크립트 인젝션 엔진 개선**: 브라우저 캐싱으로 인한 예외 상황을 막는 캐시 버스팅 로직을 강화하고, 압축된 패킷도 가벼운 바이너리 단에서 바로 풀 수 있도록 프록시 단에 디컴프레션 필터를 추가했습니다.

## [v1.6.2] - 2026-04-08

### Added (추가 기능)

- **태블릿 해상도 최적화**: 설정, 모니터, 대시보드 화면이 가로 720px 사이즈까지 온전한 레이아웃을 유지하도록 그리드 구조를 수정했습니다.

## [v1.6.1] - 2026-04-08

### Added (추가 기능)

- **인프라 통합 프록시 알림 경고**: 프록시 서버가 꺼져 있을 때 도메인 관리, 모니터링, 로그 분석 등 전체 기능 상단에 프록시 가동이 필수적임을 경고하는 통합 안내 컴포넌트(`ProxyServerWarning`)를 부착했습니다.

## [v1.6.0] - 2026-04-08

### Added (추가 기능)

- **통합 상태 토글 단추**: 모든 제어 지점에서 통일성 있는 스위치 디자인을 제공하도록 전용 `StatusToggle` 컴포넌트를 설계하여 적용했습니다.

## [v1.5.2] - 2026-04-03

### Changed (변경 사항)

- **다중 그룹 도메인 노출 개선**: 한 도메인이 여러 그룹에 속한 경우 그룹 헤더를 콤마로 나열하던 방식 대신, 각 해당 그룹 섹션마다 도메인이 각각 독립된 카드로 깔끔하게 복제되어 나오도록 개선했습니다.

## [v1.5.1] - 2026-04-03

### Added (추가 기능)

- **모달 테두리 반경 미세 조정**: 전체 모달 디자인의 일관성과 미학을 고려하여 외부 프레임 모서리의 둥글기 값을 `3xl (24px)`로 통일 적용했습니다.

## [v1.5.0] - 2026-04-03

### Added (추가 기능)

- **프로필 기반 다이내믹 테마**: 사용자가 고른 프로필 아바타 그라데이션 컬러에 어울리도록 앱의 주조색(`primary-color`)이 실시간 동적으로 깔끔하게 깔맞춤 반영되는 테마 커스텀 시스템을 적용했습니다.

## [v1.4.10] - 2026-04-02

### Added (추가 기능)

- **윈도우별 개별 상태 저장 관리**: 사용자가 여러 API 창을 띄워 놓고 작업할 때 창마다 서로 다른 검색어 및 스크롤 정보를 유지할 수 있도록 `atomWithWindowStorage` 기법을 설계하여 반영했습니다.

## [v1.4.9] - 2026-04-02

### Added (추가 기능)

- **창 분리(Detach) 지원**: 앱의 모든 하위 서브 페이지를 마우스 클릭 한 번으로 완전 분리된 독립된 윈도우 창으로 독립시킬 수 있는 타이틀바 액션을 신설했습니다.

## [v1.4.8] - 2026-03-31

### Added (추가 기능)

- **서버 로그 터미널 개발**: 러스트 백엔드 시스템 및 프록시 로그 정보를 고속으로 처리하여 터미널 형식으로 보여주는 로그 모니터를 탑재했습니다.

## [v1.4.7] - 2026-03-30

### Added (추가 기능)

- **인라인 도메인 상세 제어**: 도메인 리스트 카드 상에서 다른 대시보드로 이동하지 않고도 모니터링 상태 조정 및 즉석 프록시 경로 편집이 가능한 미니 조작 창을 적용했습니다.

## [v1.4.6] - 2026-03-30

### Added (추가 기능)

- **페이지 이동 시 상태 유지**: 사용자가 페이지를 나갔다 들어와도 이전 검색 내용이나 상세 조회 탭 정보가 유지되도록 라우터 전용 보존 구조를 구현했습니다.

## [v1.4.5] - 2026-03-27

### Added (추가 기능)

- **프로필 관리 및 온보딩**: 신규 사용자가 접속 시 초기 환경 설정을 돕는 아름다운 온보딩 마법사와 유저 이름, 역할군 설정이 가능한 프로필 설정을 릴리스했습니다.

## [v1.4.4] - 2026-03-27

### Changed (변경 사항)

- **라우트 구조 체계화**: 코드 응집도를 위해 개별 파일 기반 라우트 방식에서 폴더 인덱스(`*/index.tsx`) 구성 방식으로 전체 디렉토리를 리팩토링했습니다.

## [v1.4.3] - 2026-03-26

### Fixed (버그 수정)

- **프록시 자기 참조 순환 루프 디버깅**: 백엔드 `reqwest` 클라이언트가 윈도우 시스템 프록시를 참조하여 끊임없이 자기 자신에게 요청을 넣던 무한 루프 버그를 `.no_proxy()` 설정으로 긴급 패치했습니다.

## [v1.4.2] - 2026-02-27

### Changed (변경 사항)

- **모니터링 대상 세분화**: 기존 호스트 단독 헬스체크 방식에서 도메인 하위 서브페이지 경로별 헬스 체크가 가능하도록 모델을 고도화했습니다.

## [v1.4.1] - 2026-02-20

### Added (추가 기능)

- **API 요청/응답 기록 및 재전송(Replay)**: 트래픽 인터셉터로 캡처한 API 페이로드 정보를 바탕으로, 단 한 번의 클릭만으로 똑같은 요청 헤더 및 데이터 바디를 채워 바로 테스트 요청을 재발송해 주는 리플레이 도구를 개발했습니다.

## [v1.4.0] - 2026-02-13

### Added (추가 기능)

- **APIs 대시보드 및 OpenAPI 연동**: 각 도메인의 OpenAPI 3.0 스펙 JSON 주소를 읽어와 엔드포인트 세부 정보를 탐색하고, 즉석에서 파라미터를 입력해 테스트 발송을 처리할 수 있는 통합 API Schema Viewer를 론칭했습니다.

## [v1.3.2] - 2026-02-12

### Added (추가 기능)

- **홈 대시보드 앱 버전 연동**: Tauri 애플리케이션 정보를 읽어 대시보드 타이틀 화면에 실제 패키징 버전을 연동 노출했습니다.

## [v1.3.1] - 2026-02-11

### Fixed (버그 수정)

- **업데이터 서명 검증 키 불일치 수정**: 빌드 파이프라인에서 패키징 시 사용되던 코드 서명 키 값을 바로잡아 인앱 자동 업데이트 기능이 동작하도록 수정했습니다.

## [v1.3.0] - 2026-02-11

### Added (추가 기능)

- **실시간 자동 업데이트 공지**: 앱 부팅 시 릴리스 패키지 저장소의 태그 버전을 비교 검사하여, 최신 배포본이 있을 시 메인 배너를 통해 사용자에게 알리고 원클릭으로 패치하도록 자동화했습니다.

## [v1.2.1] - 2026-02-11

### Added (추가 기능)

- **인앱 프록시 셋업 화면**: PAC 스크립트 연동 경로 및 브라우저 보안서명용 루트 인증서 다운로드 링크를 담은 인앱 안내 페이지를 구축했습니다.

## [v1.2.0] - 2026-02-10

### Added (추가 기능)

- **환경 설정 데이터 백업 및 복원**: 도메인 데이터, 그룹 정보, 설정 파일들을 하나의 JSON 데이터 파일로 백업하여 내려받거나 이를 다시 임포트하는 기능을 추가했습니다.

## [v1.1.0] - 2026-02-10

### Added (추가 기능)

- **도메인 목록 가상 스크롤**: 도메인이 수천 개 등록되어 있어도 프레임 드랍이나 지연 시간 없이 안정적으로 렌더링되도록 행 가상화 렌더링을 적용했습니다.

## [v1.0.0] - 2026-02-09

### Added (추가 기능)

- **최초 정식 릴리스 🚀**
- 동기화 취소가 가능한 오버레이 스크린.
- 일자별 JSONL 기반 트래픽 로그 보존 및 로테이션 관리.
- 타겟 도메인 환경 셋업 및 실시간 트래픽 가로채기 엔진 탑재.
